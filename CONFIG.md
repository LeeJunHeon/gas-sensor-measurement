# CONFIG.md — config.json 채널 배정 가이드

현장에서 MFC를 PLC 모듈 채널에 배정하는 방법.

배정은 **System Setup 화면에서 드롭다운으로 편집**하거나 `config.json` 을 직접 수정한다
(둘 다 같은 파일에 저장된다). 중복·미장착 배정은 적용 시 **통째로 거부**되며,
배정을 바꾸면 안전을 위해 **모든 밸브·유량이 닫힌다**(유량 명령의 목적지가 바뀌기 때문).
밸브 코일은 채널 id로 자동 결정되어 편집 대상이 아니다.

## ① 파일 위치

`GasSensor.exe`와 같은 폴더에 있다.

```
C:\VANAM\GasSensor\
├── GasSensor.exe
├── config.json     ← 이 파일
└── recipes\
```

## ② 편집 주의

- **프로그램을 종료한 상태에서 편집할 것.** 실행 중에 편집하면 프로그램이 종료 시
  자기 메모리 값으로 파일을 덮어써 편집 내용이 사라진다.
- **편집 전에 백업**해 둔다(`config.json.bak` 등).
- JSON 문법 오류(쉼표 누락, 따옴표 짝 안 맞음)가 있으면 파일을 못 읽고
  **기본값으로 시작한다.** 설정이 모두 초기값으로 보이면 문법 오류를 의심할 것.
- 메모장보다 VSCode 등 JSON 문법을 검사해 주는 편집기를 권장한다.

## ③ 사용 가능한 채널 이름

`sv_out` / `pv_in` 에는 **아래 표의 이름만** 쓸 수 있다. 숫자를 직접 쓰지 않는다.

### SV 출력 (DV04A — MFC에 유량 지령을 내보내는 아날로그 출력)

| 이름 | 레지스터 | 물리 위치 | 비고 |
|---|---|---|---|
| `DAC1_CH0` | D100 | DV04A #1 CH0 | |
| `DAC1_CH1` | D101 | DV04A #1 CH1 | |
| `DAC1_CH2` | D102 | DV04A #1 CH2 | |
| `DAC1_CH3` | D103 | DV04A #1 CH3 | |
| `DAC2_CH0` | D104 | DV04A #2 CH0 | 증설 모듈 필요 |
| `DAC2_CH1` | D105 | DV04A #2 CH1 | 증설 모듈 필요 |
| `DAC2_CH2` | D106 | DV04A #2 CH2 | 증설 모듈 필요 |
| `DAC2_CH3` | D107 | DV04A #2 CH3 | 증설 모듈 필요 |

### PV 입력 (AD08A — MFC의 실측 유량을 읽어들이는 아날로그 입력)

| 이름 | 레지스터 | 물리 위치 |
|---|---|---|
| `ADC_CH0` | D200 | AD08A CH0 |
| `ADC_CH1` | D201 | AD08A CH1 |
| `ADC_CH2` | D202 | AD08A CH2 |
| `ADC_CH3` | D203 | AD08A CH3 |
| `ADC_CH4` | D204 | AD08A CH4 |
| `ADC_CH5` | D205 | AD08A CH5 |
| `ADC_CH6` | D206 | AD08A CH6 |
| `ADC_CH7` | D207 | AD08A CH7 |

### 밸브 코일은 config에 없다

밸브 지령 코일(VA1=160 … VA8=167, 4-way=168)은 **채널 id로 자동 결정된다.**
`config.json`에 항목 자체가 없으므로 잘못 지정할 수 없다.
화면에서 VA3을 눌렀는데 다른 밸브가 열리는 사고가 구조적으로 불가능하다.

## ③-1 현재 배정 (실배선 확정)

배선이 확정되어 아래와 같이 배정돼 있다. PLC 래더 VALVE 블록도 이 배선으로 수정·검증됐다.

| 채널 | 밸브 출력 | `sv_out` (DAC) | `pv_in` (ADC) | 비고 |
|---|---|---|---|---|
| VA1 | P40 | `DAC1_CH0` (D100) | `ADC_CH0` (D200) | Air |
| VA2 | — | `null` | `null` | **미배선** |
| VA3 | P41 | `DAC1_CH1` (D101) | `ADC_CH2` (D202) | Air |
| VA4 | — | `null` | `null` | **미배선** |
| VA5 | P42 | `DAC1_CH2` (D102) | `ADC_CH4` (D204) | Gas 1 |
| VA6 | P43 | `DAC1_CH3` (D103) | `ADC_CH5` (D205) | Gas 2 |
| VA7 | — | `null` | `null` | **미배선** |
| VA8 | — | `null` | `null` | **미배선** |

4-way는 P48로 가정한다(배선 미검증).

화면의 밸브 열(P 출력 표시)은 `plc_catalog.VALVE_OUTPUTS` 를 따른다 —
래더 VALVE 블록을 바꾸면 이 표도 함께 갱신할 것.

**VA2·VA4·VA7·VA8은 미배선** — 배선 시 `config.json` 과 `backend/state.py` 의
`DEFAULT_CHANNEL_PLC` 기본값을 **함께** 갱신할 것.
**습도 기능(VA2·VA4)은 배선 전까지 사용 불가** — 레시피의 `rh` 는 0으로 둔다.

> SV와 PV의 채널 번호가 서로 다를 수 있다(VA3는 DAC CH1 → ADC CH2). 배선이 그렇게
> 되어 있으므로 정상이다. 표의 이름을 그대로 쓰면 된다.

## ③-2 봄베와 가스 채널의 대응 (고정)

레시피의 **봄베 1~4(G1~G4)는 가스 채널 순서에 고정 대응한다.**
가스 채널이란 `grp`가 `"gas"`인 채널이며, `config.json`의 채널 순서로 결정된다.

| 봄베 | 대응 채널 | 조건 |
|---|---|---|
| 봄베 1 (G1) | 가스 채널 1번째 (기본 VA5) | |
| 봄베 2 (G2) | 가스 채널 2번째 (기본 VA6) | |
| 봄베 3 (G3) | 가스 채널 3번째 (기본 VA7) | |
| 봄베 4 (G4) | 가스 채널 4번째 (기본 VA8) | |

**채널을 꺼도 다음 채널로 대체되지 않는다.** 봄베는 물리적으로 그 밸브에 연결돼
있으므로, 대체하면 다른 농도의 봄베가 열려 계산과 실제 농도가 어긋난다.
G1을 쓰려면 VA5가 켜져 있어야 하고, 꺼져 있으면 레시피 실행이 차단된다.

봄베를 다른 밸브로 옮겼다면 `config.json`의 채널 `grp` 순서를 실제 배관에 맞춰야 한다.

## ③-3 배관 모델 — `route` (혼합 / 단독)

4-Way 밸브는 **입구가 둘**인 전환 밸브다. 한쪽은 가스 매니폴드에서 온 **혼합가스**,
다른 쪽은 4-Way로 직행하는 **단독 에어**다. 어느 쪽을 센서로 보낼지가 방향 전환이다.

| `route` | 뜻 | 배관 | 희석 계산 |
|---|---|---|---|
| `"mix"` | 혼합(희석) | 가스 매니폴드에 합류 | **포함** — 가스를 뺀 나머지 유량을 나눠 받는다 |
| `"pure"` | 단독(4-Way 직행) | 가스와 섞이지 않고 4-Way로 직행 | **제외** — 레시피가 건드리지 않는다 |

**현재 실배선**: `VA1`=혼합(희석) 에어, `VA3`=단독 에어.

**물(가습) 라인 — 잠정 페어링(위치 기준)**: `VA2`=단독 라인측 가습(`pure`),
`VA4`=혼합 라인측 가습(`mix`). 아직 배선 전이라 **위치만 보고 정한 잠정값**이다 —
실배관이 확정되면 `route` 값과 배관도 그림을 함께 재검토할 것.
(`VA2`가 `pure`인 동안 습도 배분에서 제외된다 — 희석에 쓰이는 젖은 공기는 `VA4`뿐이다.)

- **단독 라인은 레시피 소관 밖이다.** 실행 중에도 SV·밸브를 그대로 두므로,
  센서에 흘릴 에어는 **실행 전에 배관도에서 사람이 직접 맞춰 둔다.**
  (단, 완료·STOP·비상정지·트립·통신두절·종료 시에는 단독 라인도 **함께 닫힌다** — 안전은 예외 없음.)
- **PURGE(라인 청소)** 도 혼합 마른공기만 연다. 단독 라인은 가스라인을 지나지 않는다.
- **4-Way 자동 전환** — 준비(prep) 구간은 `vent`(센서에는 단독 에어만),
  측정(meas) 구간에서 `sensor`로 전환한다. 완료·STOP 후에는 `vent`로 돌아간다.
  자동 실행 중에는 수동 방향 전환이 잠긴다.

`route`는 **System Setup → 채널 구성**의 그룹 드롭다운(`Air · 혼합(희석)` /
`Air · 단독(4-way 직행)`)에서 바꾼다. ★ 실제 배관과 다르면 계산한 농도와 실제 농도가
조용히 어긋난다 — 배관을 바꿨을 때만 고친다.

### 계산 예시

전체유량 1000 sccm, G1 목표 10 ppm, 봄베1 100 ppm, 습도 미사용:

| 채널 | route | SV |
|---|---|---|
| VA5 (가스1) | mix | 1000 × 10/100 = **100** |
| VA1 (에어1) | mix | 1000 − 100 = **900** |
| VA3 (에어2) | pure | **0** (계산에서 제외 — 사람이 설정한 값 유지) |

가스도 습도도 0인 단계(에어만 흘리는 베이스라인)도 유효하다 — 혼합 마른공기가
전체유량을 그대로 받는다(위 예에서 VA1 = 1000).

## ④ 배정 방법

1. 배전반에서 각 MFC의 **설정선(SV)** 과 **출력선(PV)** 이 어느 모듈 몇 번 채널에
   물려 있는지 확인한다.
2. **System Setup → 하드웨어 배정** 표에서 해당 채널의 드롭다운을 고르고 적용한다.
   (또는 `config.json` 의 `plc.sv_out` / `plc.pv_in` 에 아래 표의 이름을 직접 쓴다.)
3. 적용 시 중복·미장착이면 거부되고 사유가 로그에 남는다 — 고쳐서 다시 적용한다.
4. 적용되면 모든 밸브·유량이 닫힌다. 배관을 확인하고 필요한 밸브를 다시 연다.

예시 — 가스1(VA5)의 MFC 설정선이 DV04A #1 CH2, 출력선이 AD08A CH4에 물려 있다면
(현재 실배선):

```json
{
  "id": "VA5", "grp": "gas", "route": "mix", "en": true, "max": 2000, "sv": 0.0,
  "plc": {
    "sv_out": "DAC1_CH2",
    "pv_in": "ADC_CH4",
    "fs_sccm": 2000, "sv_full": 2000, "pv_zero": 0, "pv_full": 4000
  }
}
```

같은 이름을 두 채널이 쓰면 **중복**이므로 한쪽을 옮겨야 한다. 중복이면 시작 시 경고가 뜬다.

배정하지 않을 채널은 `null` 로 둔다(따옴표 없이).

```json
"sv_out": null
```

## ④-2 아날로그 스케일 — `sv_full` 은 래더와 짝이다

채널 `plc` 의 스케일 4키는 sccm ↔ 모듈 카운트 환산에 쓰인다.

**MFC 명판 (확인 완료)**

| 항목 | 값 |
|---|---|
| 모델 | HORIBA **S48-BR221** |
| 풀스케일 | **1000 sccm** |
| 설정 신호(입력) | **0~5 V** |
| 출력 신호(PV) | **0~5 V** |
| 수량 | 4대 (VA1·VA3·VA5·VA6) — **전부 동일 사양** |

★ **AD08A 입력범위는 `0~5V` 여야 하며 MFC 출력선은 전압 단자(V+)에 결선한다.
0~20mA 설정으로는 PV가 읽히지 않는다.**

| 키 | 현재값 | 의미 |
|---|---|---|
| `fs_sccm` | `1000` | MFC 하드웨어 풀스케일(sccm). 명판 확인 완료 |
| `sv_full` | `2000` | 풀스케일에 해당하는 DAC 카운트(현재 0~5V 가정) |
| `pv_zero` | `0` | 유량 0일 때 ADC 카운트 |
| `pv_full` | `4000` | 풀스케일일 때 ADC 카운트(AD08A 0~4000) |
| `gas_name` | `"Air"`/`"N2"` | **전 채널**. 봄베의 **베이스 기체** 이름(표시·참고용) |
| `gas_cf` | `1.0` | **전 채널**. 변환계수(C.F.). 계산에 실제로 쓰이는 값 |

### PV 무유량 잡음 데드밴드 (`pv_deadband`)

밸브를 모두 닫고 비상정지한 상태인데도 PV 가 `1 sccm`, TOTAL FLOW 가 `1`로 뜨고
배관도에 흐름이 점등된 적이 있다(2026-08-19 실기). ADC 의 무유량 잡음 4카운트가
유량으로 환산된 것이다. 실기에서 확인된 무유량 잡음은 **±5카운트 수준으로 정상 범위**다.

- **단위는 ADC 카운트**(sccm 이 아니다). 잡음은 ADC 레벨 현상이라, 풀스케일이 다른 채널에
  같은 sccm 문턱을 쓰면 한쪽은 과하고 한쪽은 모자란다.
- **기본 8** — `pv_zero` 로부터 ±8카운트 안이면 `0.00 sccm` 으로 본다.
  8카운트 = 0.2 %F.S. 로 **MFC 정확도 ±1 %F.S. 보다 작아** 유효 정보 손실이 없다.
- 적용 지점은 **보정표 조회·선형 변환보다 앞**이다. 보정표가 있는 채널도 동일하게 걸린다.
- **표시·TOTAL FLOW·배관도 점등이 모두 이 값을 통과한 PV 를 쓴다** —
  흐름 점등 판정은 지금도 '실측 PV > 0' 기준이다(지령 기준이 아니다).
- **조정**: 잡음이 더 크면 값을 키우고, 아주 낮은 유량까지 봐야 하면 줄인다(`0` 이면 끔).
  System Setup 에는 넣지 않았다 — 현장 조정 빈도가 낮아 `config.json` 을 직접 고친다.

```json
"plc": { "...": "...", "pv_zero": 0, "pv_full": 4000, "pv_deadband": 8 }
```

### PV 보정표 (`calib/*.csv`)

실기에서 MFC 유량 출력이 **풀스케일의 약 63%(≈3.2V)에서 포화**한다(원인 조사 중 —
전원·유량 포화는 배제됨). 그 상태에서도 PV 가 전 구간에서 실제 유량에 대응하도록
실측 DAC-ADC 매핑을 표로 두고 **구간 선형 보간**한다.

> ⚠ **현 표는 출력 포화 상태의 실측이다.** 배선·모듈 원인이 해결되면 **재측정해 교체할 것.**
> SV(지령) 경로에는 쓰지 않는다 — DAC 는 전 구간 선형임이 유량계로 확인됐다.

- **위치·파일명**: `calib/<채널ID>.csv` (예: `calib/VA5.csv`). 찾는 순서는
  ① **exe 옆 `calib/`(현장 교체본 — 우선)** → ② `_internal/calib/`(빌드 동봉 기본값).
  어느 쪽이 쓰였는지는 로그에 남는 **전체 경로**로 판별한다
  (재빌드 후 옛 현장 파일이 새 번들값을 덮는 혼선을 막기 위한 것이다).
  개발 환경에서는 두 경로가 같은 폴더다.
  파일이 **없는 채널은 기존 선형식**(`pv_zero`~`pv_full`)을 그대로 쓴다.
- **형식** — 두 열이면 된다. 첫 줄이 헤더다.

  | 헤더 | 첫 열의 뜻 |
  |---|---|
  | `dac,adc` | DAC 카운트 (내부에서 `sccm = dac ÷ sv_full × fs_sccm` 로 환산) |
  | `sccm,adc` | 유량(sccm) 그대로 |

  헤더가 없으면 `dac,adc` 로 본다. 구분자는 쉼표·탭·세미콜론 모두 되고,
  엑셀 저장본의 BOM·CRLF·따옴표도 그대로 읽는다(표준 csv 파서를 쓴다).
- **주석**: `#` 으로 시작하는 줄과 빈 줄은 무시한다. 엑셀은 주석에 쉼표가 있으면 줄 전체를
  큰따옴표로 감싸므로(`"# MFC1 PV 보정 — 2026-08-19 실측, AD08A 0~5V 설정",`)
  따옴표를 벗긴 뒤 판단한다. 첫 칸이 숫자가 아닌 줄(제목·설명)은 조용히 건너뛴다.
- **⚠ 채널당 한 장으로 나눌 것**: 여러 채널을 나란히 담은 엑셀 원본을 그대로 저장하면
  **앞 두 열만** 읽어 엉뚱한 채널의 곡선이 조용히 걸린다. 숫자 열이 3개를 넘으면
  로그에 경고를 남기니 그때는 앞 두 열이 그 채널 것인지 확인한다.
- **로그 예**: `PV 보정표 로드 — VA5 21점 (C:\VANAM\GasSensor\calib\VA5.csv)`
- **자동 보완**: 첫 점의 adc 가 `pv_zero` 보다 크면 맨 앞에 `(pv_zero, 0)` 을 넣는다
  (무유량 지점이 없으면 저유량 구간이 정의되지 않는다).
- **검증** — 하나라도 어긋나면 그 채널은 **경고 1줄 + 선형 폴백**이다.
  · 유효한 점 2개 이상 · adc 열이 **강하게 증가** · 유량 열이 비감소 · 숫자 파싱 가능
  포화 구간에서 adc 가 같은 값으로 반복되면(예: `2574` 가 두 번) 되돌릴 수 없으므로
  **중복 행을 지운다.**
- **반영 시점**: 기동 시 1회, 그리고 **`통신 재연결`** 을 누를 때마다 다시 읽는다 —
  실기 조정 중 프로그램을 껐다 켜지 않고 CSV 를 교체·수정해 바로 확인할 수 있다.
  파일이 있는 채널만 `PV 보정표 로드 — VA5 21점 (calib/VA5.csv)` 로 한 줄 남는다.
- **표가 있으면 `pv_zero`/`pv_full` 은 무시된다** — 표가 그 역할을 대신한다.
  `gas_cf`(C.F.)는 표 조회 **결과에** 곱해진다(선형 경로와 같은 위치).
- **범위 밖**: 표의 마지막 adc 를 넘으면 **마지막 유량으로 클램프**한다(외삽 금지 —
  평탄부에서 기울기를 늘리면 유량이 발산한다).
- **분해능 한계**: 출력이 포화하는 **고유량 구간은 adc 변화가 매우 작아**
  (예: 1300→2000 DAC 구간에서 adc 가 2477→2529, 52카운트뿐) 표시 유량의 분해능이 낮다.
  이 구간의 PV 는 참고값으로 보고, 정밀 확인은 유량계로 한다.

### 가스 변환계수 (C.F.)

열식 MFC(HORIBA S48)는 **전 채널 N2 로 교정**되어 출고된다. 다른 기체를 흘리면 지시값과
실제 유량이 달라지므로 매뉴얼의 변환계수로 보정한다.

```
매뉴얼:  실제 유량 = 설정 유량 ÷ (교정가스 C.F.) × (사용가스 C.F.)
교정가스가 N2(1.000) 이므로 이 장비에서는
         실제 유량 = 지시 유량 × gas_cf
         지시(SV)  = 목표 유량 ÷ gas_cf
```

- **표 출처**: HORIBA S48(M)-BR/BM 매뉴얼 CONVERSION FACTOR 표(N2 교정 기준) 30종을
  `backend/gas_catalog.py` 에 내장했다. 표에 없는 가스는 **호리바에 문의**할 것 —
  제조사마다 기준 온도·환산식이 달라 타사 표를 섞어 쓰면 값이 어긋난다.
  (매뉴얼 예시 1의 "H2 → 1.4" 문장은 같은 매뉴얼 표의 `H2 = 1.012` 와 모순이라 오타로 본다.)
- **ppm 급 혼합가스**는 희석된 성분이 아니라 **베이스 기체**의 C.F. 를 쓴다.
  (예: N2 베이스 100 ppm NO 봄베 → `N2`, `1.000`)
- **수정 규칙**: System Setup 의 스케일 표에서 기체를 고르면 표 값이 자동으로 채워지고,
  그 뒤 값을 직접 고칠 수 있다(교정 성적서 값 등). 표 값과 다르면 `(수정됨)` 이 표시된다.
  허용 범위는 `0.1 ~ 10`, 범위 밖이면 저장이 거부된다.
- **적용 지점은 한 곳**이다 — sccm ↔ raw 변환(`plc.py`). 레시피 표·희석 계산·`MAX` 는
  전부 **실제 유량(sccm)** 기준이므로 이중 보정이 일어나지 않는다.
- 실행 전 검사는 **지시 유량 기준**으로 막는다. C.F. < 1 인 기체는 목표가 풀스케일 안이어도
  지시가 넘쳐 조용히 클램프되기 때문이다
  (예: 풀스케일 200, CO2 0.735 → 목표 150 sccm 은 지시 204 sccm 이라 거부).
- **전 채널(에어 포함)이 값을 가진다**(v1.2.1). 에어 채널 기본값은 `Air`/1.000,
  가스 채널은 `N2`/1.000 — 둘 다 1.000 이라 보정이 항등이고 기존 동작과 같다.
  에어 봄베가 순수 Air 가 아니면 그 행의 C.F. 를 고치면 된다.
- 표에 없는 가스는 드롭다운에서 **직접 입력**을 고르면 이름 입력칸으로 바뀐다.
  이름을 쓰고 우측 C.F. 에 값을 넣는다(이름이 비면 저장이 거부된다).
  `▾`(표에서 선택) 버튼을 누르면 드롭다운으로 돌아가고 N2/1.000 이 채워진다.

※ 화면·telemetry 의 `pv_raw` 는 **부호 있는 값**이다(입력이 개방되면 −48 같은 음수가 나온다).
무부호로 읽으면 65488 이 되어 16372 sccm 같은 허수 유량이 표시되므로, 서버가 16비트
부호 변환을 거쳐 내려준다. 유량(sccm) 값은 음수를 0.0 으로 잘라 표시한다.

★ **MFC 설정 입력이 0~10V 사양으로 확정되면 두 곳을 함께 바꾼다** —
① `config.json` 의 `sv_full` 2000→4000,
② PLC 래더 SV_WRITE 블록의 `D00301` 초기값 2000→4000
(변수명 없음 — "SV 제한상수 초기화" 런그의 `MOV 2000 D00301`).
**한쪽만 바꾸면 유량이 절반 또는 2배로 틀어진다.**

## ⑤ DV04A 증설 시

DV04A가 1대뿐이라 SV 출력은 4채널이고, 현재 VA1·VA3·VA5·VA6이 이를 나눠 쓴다.
미배선인 VA2·VA4·VA7·VA8까지 8채널을 모두 쓰려면 증설이 필요하다.

1. PLC 슬롯3에 DV04A 모듈을 장착한다.
2. XG5000에서 `SV_WRITE2` 블록의 **"프로그램에서 제외"를 해제**하고 다운로드한다.
3. `config.json`의 `plc_hw.dac_modules` 를 `2` 로 바꾼다.

   ```json
   "plc_hw": { "dac_modules": 2 }
   ```

   `plc_hw`는 최상위 블록이다(`plc_system`은 Modbus 주소 전용이라 여기에 없다).
4. 새로 배선한 채널(VA2·VA4·VA7·VA8)의 `sv_out` 을 `DAC2_CH0` ~ `DAC2_CH3` 으로 배정한다.

`dac_modules`가 `1`인 상태에서 `DAC2_*`를 지정하면 시작 시 경고가 뜬다
(실제로 나갈 곳이 없기 때문).

## ⑤-2 하드웨어 구성 (`plc_hw`)

최상위 `plc_hw` 블록은 주소가 아니라 장비 구성값이다.

| 키 | 기본값 | 설명 |
|---|---|---|
| `dac_modules` | `1` | DV04A 장착 수. 증설하면 `2` |
| `v4w_on_is_sensor` | `true` | 4-Way 코일 ON이 **측정(sensor) 방향**인가 |

```json
"plc_hw": { "dac_modules": 1, "v4w_on_is_sensor": true }
```

**실기에서 4-Way를 전환했는데 반대로 동작하면** (화면은 Sensor인데 실제로는 Vent로
흐르는 등) `v4w_on_is_sensor` 를 `false` 로 바꾼다. 코드 수정이나 재빌드가 필요 없다.

> **확정(2026-08-12)**: 실물 4-Way의 무전원(코일 OFF) 위치는 **가스→Vent / 에어→Sensor** 다.
> 래더 VALVE 런그 13이 `V4W_OUT(P00048) = V4W_CMD(M00108) AND RUN_PERMIT(M00203)` 이므로
> 트립·정전 시 자동으로 이 안전 위치가 된다. 따라서 `v4w_on_is_sensor: true` 가 정답이다.
> 다만 **P48 배선이 실제로 그 밸브에 연결돼 있는지는 현장 검증 항목**으로 남아 있다 —
> 실기에서 반대로 동작하면 이 값을 `false` 로 바꾼다(코드 수정·재빌드 불필요).

## ⑥ 문제 진단

| 증상 | 확인할 것 |
|---|---|
| 밸브는 열리는데 가스가 안 나옴 | `sv_out` 미배정 확인 (System Setup의 배정 표에 "⚠ SV 출력 없음") |
| PV가 항상 0 | `pv_in` 배정 확인, AD08A 배선 확인 |
| 화면 값과 실제 유량이 다름 | `fs_sccm` / `sv_full` 이 MFC 명판과 맞는지 확인 |
| 두 채널이 같이 움직임 | 같은 `sv_out`을 두 채널이 쓰고 있는지 확인(중복) |
| 4-Way가 반대로 동작 | `plc_hw.v4w_on_is_sensor` 를 `false` 로 (⑤-2 참고) |
| 시작 시 경고 메시지가 뜸 | 메시지 내용대로 조치. 경고가 있어도 프로그램은 실행된다 |
| 설정을 바꿔도 재시작하면 초기값 | 설치 경로에 쓰기 권한이 있는지 확인(`BUILD.md`의 "설치 위치 주의"). 시작 시 "데이터 폴더에 쓸 수 없습니다" 안내가 뜬다 |
| 레시피 실행이 거부됨 | 유량이 배분된 채널에 `sv_out`이 없을 때 차단된다 |

- 연결이 주기적으로 끊겼다 붙으면: USB 시리얼 어댑터의 **응답 시간 조정(Latency Timer)**을
  1 ms로, 수신 버퍼를 64바이트로 낮춘다(장치관리자 → 포트 속성 → 고급).
  프로그램은 연속 3회 실패부터 끊김으로 판정한다(`plc.POLL_FAIL_LIMIT`).
  단발 실패는 화면 로그에 뜨지 않고 `measurement-*.log` 에 "읽기 실패 n/3 — 재시도"로만 남는다.
  하트비트 단발 실패도 0.2초 후 1회 재시도로 흡수된다(`measurement-*.log` 의 "재시도 성공").
  이 줄이 반복되면 Latency Timer 1 ms 적용 여부를 먼저 확인한다.
  ★ 이 재시도는 `timeout_s` 가 1.5 이하일 때만 COMM_TMR(3초) 안에서 끝난다 — 시리얼은 1.5 이하 권장.

### 연결 시 운전 상태 인수(takeover)

프로그램이 PLC 에 붙는 순간(끊김→연결 전이) **운전허가가 켜져 있으면** 밸브 지령·SV·4-way 를
PLC 에서 읽어 그대로 이어받는다(HMI 재시작·`test_plc` 인수 후에도 흐르던 가스가 끊기지 않는다).
**안전정지(트립) 상태면 인수하지 않고 기존대로 닫힘으로 동기화한다** — 콜드 부팅·세션 사망 뒤의
스테일 지령을 이어받으면 '운전 준비'를 누르는 순간 이전 밸브가 자동으로 열리기 때문이다.
설정상 꺼진(`en=false`) 채널은 PLC 에 열려 있어도 인수하지 않고 다음 쓰기에서 닫는다(파일 로그에 기록).
정상 주기 요청 수는 그대로다(읽기 2요청) — 인수 읽기(코일 9 + 레지스터 4)는 **연결 전이 1회**뿐이다.

시작 시 경고는 콘솔과 화면의 System Log 양쪽에 남는다.

## ⑥-2 공압 인터록 제거 (2026-08-11)

**사유** — 공압 센서 신호를 읽을 수 없어, PLC 래더에서 공압 관련 로직
(`AIR_SGN` · `AIR_OK` · `AIR_TMR` · `ALM_AIR`)을 삭제했다.

**현재 자동 트립 조건은 통신 하트비트 3초 두절 하나뿐이다.**
⚠️ 에어 공급 이상은 더 이상 자동으로 차단되지 않는다 — 운전자가 직접 감시해야 한다.

**HMI 쪽 정리** — 상태 키 `AIR_OK` · `ALM_AIR`(및 `air_ok` · `alm_air` 별칭),
PLC 상태 패널의 `공압` · `Air알람` 표시, fake_plc 의 `air` 명령을 제거했다.
모드버스 주소 **320(M00200) · 336(M00210) 은 복원용으로 예약**해 둔다 —
다른 용도로 재사용하지 말 것. 상태 코일 블록 읽기 범위도 `read_coils(320, 20)`
그대로 유지하고 있다(프레임 불변).

**복원 절차**

1. 공압 센서를 `P00000` 에 결선한다.
2. SAFETY 블록에 런그 2개를 복원한다 — `AIR_SGN → AIR_OK`, `TON T0000 100`.
3. 트립 런그에 `T0000` 병렬 접점을, arm 런그에 `P00000` 직렬 접점을 추가한다.
4. ALARM 블록에 공압 알람 런그를 추가한다.
5. 변수 4행(`M00200` · `M00210` · `P00000` · `T0000`)을 재등록한다.
6. HMI 상태 표시·키 맵을 복원한다(코일 320/336 예약을 그대로 쓰면 된다).

## ⑦ 편집 권한

배정 편집에 별도의 비밀번호는 없다(관리자 잠금 계획은 폐기). 안전은 구조로 지킨다 —
드롭다운(오타 불가) + 중복·미장착 검증(통째 거부) + 배정 변경 시 전체 닫힘.
`dac_modules` 와 밸브 코일 배정은 화면에서 바꿀 수 없다(⑤·③ 참고).
